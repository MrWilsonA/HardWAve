{
  "name": "hardwave-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "build:frontend": "npm run build --prefix frontend",
    "build:contracts": "npm run build --prefix contracts",
    "test:contracts": "npx hardhat test --prefix contracts",
    "build": "npm run build:frontend"
  }
}
